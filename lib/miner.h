#include <stdio.h>
#include <stdlib.h>
#include <time.h>


int mine(){
	time_t t;
	srand((unsigned) time(&t));
	float coins;
	FILE *xman;
	xman = fopen("/home/EAC.txt","w+");
	while(1){
		
	fclose(xman);
}